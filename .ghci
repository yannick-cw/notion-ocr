:set -fwarn-unused-binds -fwarn-unused-imports
:set -isrc
:set -Wall
:set -Wincomplete-uni-patterns
:set -Wincomplete-record-updates
:set -Wcompat
:set -Widentities
:set -Wredundant-constraints      (ghc >= 8.0)
:set -fhide-source-paths          (ghc >= 8.2.2)

:load Main
:load LibSpec
